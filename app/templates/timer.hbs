<TimerPage
  @timeLeft={{this.timer.currentTime}}
  @totalTime={{this.timer.totalTime}}
  @paused={{this.timer.paused}}
  @start={{this.start}}
  @pause={{this.pause}}
  @label={{this.timer.step.label}}
  @round={{this.timer.round}}
  @rounds={{this.timer.rounds}}
/>