import duration from 'pomodoro-timer/utils/duration';
import { module, test } from 'qunit';

module('Unit | Utility | duration', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = duration();
    assert.ok(result);
  });
});
